# テスト結果レポート — 2026-04-19 22:43

対象コミット: `3c5a534` + 本セッション変更（未コミット）
実施フェーズ: **Phase QA-T2（Vitest ユニット）** + **Phase QA-CI（GitHub Actions 設定）**
T3 / T4: ブロッカー確認・未実施

---

## 0. サマリ

| Phase | 状態 | 実施 | 合格 | 不合格 | 備考 |
|---|---|---:|---:|---:|---|
| **QA-T2 Vitest** | ✅ 完了 | 43 | 43 | 0 | 4 ファイル・43 テスト |
| **QA-T3 API 統合** | ⏳ ブロッカーあり | — | — | — | テスト用 DB とセッションモック未整備 |
| **QA-T4 Playwright E2E** | ⏳ ブロッカーあり | — | — | — | chromium install + OAuth モック未整備 |
| **QA-CI GitHub Actions** | ✅ 設定ファイル作成完了 | — | — | — | `.github/workflows/test.yml` 配置済み（push で有効化） |

### 追加の静的解析
| 項目 | 結果 |
|---|---|
| `tsc --noEmit` | ✅ 0 エラー |
| `next lint` | ✅ warnings/errors なし |

**総合判定**: 🟢 **PASS**（実施可能な範囲は全件合格）

---

## 1. Phase QA-T2: Vitest ユニットテスト

### 1.1 セットアップ
```bash
pnpm add -D vitest @vitest/ui
```
- Vitest v4.1.4
- 設定ファイル: `vitest.config.ts`（node 環境・`@/` エイリアス・`test/**/*.test.ts` パターン）
- package.json scripts 追加: `test` / `test:watch` / `test:ui` / `test:smoke`

### 1.2 リファクタリング（テスト可能化）

| 変更 | 目的 |
|---|---|
| `lib/repositories/articles.ts`: `escapeLike` を関数外に移動＋ export | ILIKE エスケープ関数を単独でテスト可能に |
| `lib/schemas/articles.ts`（新規） | `createArticleSchema` / `updateArticleSchema` / `optionalDate` を独立ファイル化 |
| `app/api/admin/articles/route.ts` | zod スキーマを新ファイルから import に変更 |
| `app/api/admin/articles/[slug]/route.ts` | 同上 |

→ API route の挙動は従前と同一（内部リファクタリングのみ）。`tsc` + `lint` で検証済み。

### 1.3 テスト結果

```
 Test Files  4 passed (4)
      Tests  43 passed (43)
   Duration  363ms
```

#### 1.3.1 `test/unit/buildQueryString.test.ts` — 10/10 ✅

| ID | テスト内容 | 結果 |
|---|---|:---:|
| V1 | 単一値 `?k=v` | ✅ |
| V1b | 複数単一値 | ✅ |
| **V2** | **配列 → 繰り返しパラメータ展開（Rev22 cat複数対応）** | ✅ |
| V2b | 3要素配列 | ✅ |
| V3 | undefined / null 省略 | ✅ |
| V3b | 空オブジェクト | ✅ |
| V3c | boolean 文字列化 | ✅ |
| V3d | number 文字列化 | ✅ |
| V3e | 空配列はキー省略 | ✅ |
| V3f | URL エンコード | ✅ |

#### 1.3.2 `test/unit/escapeLike.test.ts` — 8/8 ✅

| ID | テスト内容 | 結果 |
|---|---|:---:|
| E2a | 通常文字列 | ✅ |
| **E2b** | **`%` エスケープ** | ✅ |
| **E2c** | **`_` エスケープ** | ✅ |
| **E2d** | **`\` エスケープ** | ✅ |
| E2e | 混合文字列 `50%_off` | ✅ |
| E2f | 空文字 | ✅ |
| E2g | 日本語は無影響 | ✅ |
| E2h | SQL インジェクション試行もリテラル扱い | ✅ |

#### 1.3.3 `test/unit/createArticleSchema.test.ts` — 17/17 ✅

| ID | テスト内容 | 結果 |
|---|---|:---:|
| Z1a | 最小必須で合格 + デフォルト値 | ✅ |
| Z1b〜d | slug / title / body 欠落で失敗 | ✅ ✅ ✅ |
| Z1e | 空文字列 title で失敗 | ✅ |
| Z1f | **audioDurationSec に NaN で失敗**（Rev22 焦点） | ✅ |
| Z1g | audioDurationSec 負数で失敗 | ✅ |
| Z1h | audioDurationSec 120 で合格 | ✅ |
| Z2a | 不正 role enum で失敗 | ✅ |
| Z2b | 正常 role enum 合格 | ✅ |
| Z2c | 不正 category enum で失敗 | ✅ |
| Z2d | level='advanced' 合格 | ✅ |
| Z2e | 不正 level で失敗 | ✅ |
| Z3a | publishedAt='' → null | ✅ |
| Z3b | publishedAt=null → null | ✅ |
| Z3c | publishedAt='2026-04-19' → Date | ✅ |
| Z3d | 不正日付 → null | ✅ |

#### 1.3.4 `test/unit/updateArticleSchema.test.ts` — 8/8 ✅

| ID | テスト内容 | 結果 |
|---|---|:---:|
| Z4a | 空オブジェクト成功（全 optional） | ✅ |
| Z4b | title のみ更新 | ✅ |
| Z4c | published のみ更新 | ✅ |
| Z4d | 空文字列 title で失敗 | ✅ |
| Z4e | 不正 role enum で失敗 | ✅ |
| Z4f | description=null 上書き可 | ✅ |
| Z4g | publishedAt='' → null | ✅ |
| Z4h | audioDurationSec 小数で失敗 | ✅ |

---

## 2. Phase QA-T3: API 統合テスト — ⏳ ブロッカー

### 2.1 未実施理由

| ブロッカー | 対応策 | 見積 |
|---|---|---|
| テスト用 DB 未整備 | Neon branch を create → `.env.test` を用意 → drizzle migrate 実行 | 60〜90 分 |
| NextAuth セッションのモック手段未構築 | `vi.mock('@/lib/auth')` で `auth()` を直接モック or `next-test-api-route-handler` 導入 | 60 分 |
| Upstash Ratelimit のフェイク | メモリ内実装を DI で差し替え（`lib/ratelimit.ts` のリファクタが必要） | 30 分 |
| シード/クリーンアップ戦略 | beforeEach で TRUNCATE、afterAll で Neon branch drop | 30 分 |

### 2.2 実施時のテスト対象（test-plan_v1.md 再掲）
- G1〜G11（admin API 全 5 ルート × 正常/異常系）
- F1〜F8（公開 API 正常系）
- H1〜H3（AI API × free/pro プラン分岐）
- **D1〜D2（rate limit 10req/min → 429）** ← Rev23 重点検証

### 2.3 推奨着手タイミング
Phase 1 リリース後、Rev24 着手前。見積合計 **3〜4 時間**。

---

## 3. Phase QA-T4: Playwright E2E — ⏳ ブロッカー

### 3.1 未実施理由

| ブロッカー | 対応策 | 見積 |
|---|---|---|
| `@playwright/test` 未インストール（150MB） | `pnpm add -D @playwright/test && playwright install chromium` | 10 分 |
| dev server 自動起動 | `playwright.config.ts` の `webServer` 設定 | 15 分 |
| Google OAuth のモック | `providers: [Credentials]` のみでテスト or OIDC モックサーバ | 60〜90 分 |
| 管理者セッションの保存 | `storageState` に管理者ログイン後の Cookie を焼き込む | 30 分 |
| テスト記事の seed | `e2e/fixtures/articles.ts` で既知データを投入 | 30 分 |

### 3.2 実施時のテスト対象（test-plan_v1.md §5.2 再掲）
1. ゲスト記事閲覧（トップ → /learn → 詳細 → 戻る）
2. 検索
3. ロールフィルタ
4. Credentials 新規登録 + ログイン
5. Google OAuth（モック）
6. AI チャット free プラン → 429
7. 管理画面 CRUD（作成 → 編集 → 公開切替 → 削除）
8. ダブルクリック防止（Rev22）
9. CSRF 403
10. 404 / エラー画面

### 3.3 推奨着手タイミング
QA-T3 完了後。見積合計 **4〜6 時間**。

---

## 4. Phase QA-CI: GitHub Actions 設定 — ✅ 完了

### 4.1 配置ファイル
`.github/workflows/test.yml`

### 4.2 有効化済みジョブ: `static-and-unit`

| ステップ | 内容 |
|---|---|
| 1 | checkout |
| 2 | pnpm v10 セットアップ |
| 3 | Node 20 + pnpm キャッシュ |
| 4 | `pnpm install --frozen-lockfile` |
| 5 | `pnpm exec tsc --noEmit` |
| 6 | `pnpm lint` |
| 7 | **`pnpm test`**（Vitest） |

`push`（main）と `pull_request`（main 向け）でトリガ。

### 4.3 コメントアウト済みジョブ: `smoke`

スモーク（Phase T1）は次世代の生存確認用。有効化には以下が必要:
- GitHub Secrets 登録: `DATABASE_URL`, `NEXTAUTH_SECRET`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, 他
- `wait-on` パッケージ追加

### 4.4 将来の拡張
```
jobs:
  ├─ static-and-unit    (✅ 有効)
  ├─ smoke              (準備中・secrets 登録で有効化)
  ├─ integration        (QA-T3 完了後に追加)
  └─ e2e                (QA-T4 完了後に追加・Playwright)
```

---

## 5. 追加変更ファイル一覧

### 新規作成
- `vitest.config.ts`
- `lib/schemas/articles.ts`
- `test/unit/buildQueryString.test.ts`
- `test/unit/escapeLike.test.ts`
- `test/unit/createArticleSchema.test.ts`
- `test/unit/updateArticleSchema.test.ts`
- `.github/workflows/test.yml`
- `todo/TestResult_202604192243.md`（本ファイル）

### 変更
- `package.json`（scripts 4件追加、devDeps: vitest/vitest-ui）
- `pnpm-lock.yaml`（vitest 追加分）
- `lib/repositories/articles.ts`（`escapeLike` 外出し + export）
- `app/api/admin/articles/route.ts`（zod を schemas から import）
- `app/api/admin/articles/[slug]/route.ts`（同上）

---

## 6. 実行コマンド早見表

| 用途 | コマンド |
|---|---|
| ユニットテスト 1回実行 | `pnpm test` |
| ウォッチモード | `pnpm test:watch` |
| UI モード | `pnpm test:ui` |
| スモークテスト | `BASE_URL=http://localhost:3000 pnpm test:smoke` |
| 型検査 | `npx tsc --noEmit` |
| Lint | `pnpm lint` |

---

## 7. 次アクション

| # | アクション | 所要時間 | 優先度 |
|---|---|---:|---|
| 1 | 本変更を commit + push（CI 初回発火） | 5分 | **High** |
| 2 | QA-T3: Neon branch 作成 + セッションモック基盤構築 → API統合テスト | 3〜4h | Med |
| 3 | QA-T4: Playwright install + Credentials ベースで主要10シナリオ | 4〜6h | Med |
| 4 | QA-CI 拡張: smoke ジョブ有効化（Secrets 設定後） | 30分 | Low |

---

## 8. 環境情報

| 項目 | 値 |
|---|---|
| 日時 | 2026-04-19 22:43 JST |
| ブランチ | main |
| Node.js | v20.x |
| pnpm | 10.7.1 |
| Vitest | 4.1.4 |

---

以上
