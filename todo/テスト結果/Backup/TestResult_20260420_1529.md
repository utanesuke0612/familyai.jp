# TestResult 2026-04-20 15:29 — Rev24 #①④⑤ + QA-T3 admin CRUD 部分完了

## サマリー

| 項目 | 結果 |
|---|---|
| 実行日時 | 2026-04-20 15:29:48 JST |
| ツール | Vitest v4.1.4 |
| Node | macOS（Codex 側 Windows/OneDrive は運用ノート参照）|
| Test Files | **9 passed (9)** |
| Tests | **79 passed (79)** |
| Duration | 694ms（transform 733ms / setup 0ms / import 1.47s / tests 268ms / environment 1ms）|
| `tsc --noEmit` | EXIT=0 |
| `pnpm lint` | ✔ No ESLint warnings or errors |

## 追加履歴

| 日付 | Rev | テスト数 | 増分 |
|---|---|---:|---|
| 2026-04-19 | QA-T2 初回 | 43 | Vitest 導入・utils/zod/escapeLike |
| 2026-04-20 | Rev27 | 46 | `shared-api.contract.test.ts`（+3）|
| 2026-04-20 | Rev24 #① | 49 | `shared-api.latest-related.test.ts`（+3）|
| 2026-04-20 | Rev24 #④ | 55 | `adminArticlesQuerySchema.test.ts`（+6）|
| 2026-04-20 | Rev24 #⑤ | 65 | `csrf.test.ts`（+10）|
| 2026-04-20 | QA-T3 admin CRUD | **79** | `admin-articles.integration.test.ts`（+14）|

## ファイル別内訳

### test/unit/buildQueryString.test.ts（10 件）
- V1・V1b: 単一値の ?k=v 形式
- V2・V2b: 配列→繰り返しパラメータ展開（Rev22 cat 複数対応）
- V3a〜V3f: undefined/null 省略・boolean/number 文字列化・空配列省略・URL エンコード

### test/unit/escapeLike.test.ts（8 件）
- E2a〜E2h: ILIKE メタ文字 %・_・\ のエスケープ、日本語・SQLi 的試行

### test/unit/createArticleSchema.test.ts（17 件）
- Z1a〜Z1h: 必須フィールド検証・NaN reject・audioDurationSec 範囲
- Z2a〜Z2e: role/category/level enum 検証
- Z3a〜Z3d: publishedAt 空文字/null/日付変換

### test/unit/updateArticleSchema.test.ts（8 件）
- Z4a〜Z4h: 全 optional・null 上書き・publishedAt 空文字→null

### test/unit/shared-api.contract.test.ts（3 件・Rev27 #1）
- 200 `{ok,data}` → `data` 1 枚剥がし
- 4xx `{ok:false,error:{message}}` → `error.message` に message が入る
- ネットワーク例外 → `NETWORK_ERROR`

### test/unit/shared-api.latest-related.test.ts（3 件・Rev24 #①）
- `fetchLatest(baseUrl, 6)` → `/api/articles/latest?limit=6`
- `fetchRelated(baseUrl, slug, 3)` → `/api/articles/:slug/related?limit=3`
- `fetchRelated` 404 → `error.message / status=404 / code=NOT_FOUND`

### test/unit/adminArticlesQuerySchema.test.ts（6 件・Rev24 #④）
- デフォルト値（sort=latest / page=1 / pageSize=50）
- `page`/`pageSize` の coerce（文字列→数値）
- `page=0` 違反 reject / `pageSize=201` 違反 reject
- sort enum 外 reject / search の trim + 1-100 文字チェック

### test/unit/csrf.test.ts（10 件・Rev24 #⑤）
- 既存 Origin チェック: Origin なし/一致/不一致/localhost（4 件）
- `verifyMobileClient` / `verifyCsrf(allowMobile:true)`:
  - `MOBILE_API_KEYS` 未設定時は常に false
  - 有効な ios/android key → true
  - サポート外 platform → false
  - 許可リスト外 key → false
  - `allowMobile:true` + 有効 key なら Origin 不一致でも true
  - `allowMobile:false` デフォルトでは admin API 同等に Origin で判断

### test/integration/admin-articles.integration.test.ts（14 件・QA-T3）

**GET /api/admin/articles**
- 200 + `data.items`/`data.meta`（page/pageSize/total/totalPages）
- 403 非管理者
- 403 未ログイン
- 400 不正な sort（`random`）

**POST /api/admin/articles**
- 201 正常作成
- 403 CSRF 違反（Origin 不一致）
- 400 zod validation 失敗（slug 欠落）
- 429 rate limit（`enforceAdminRateLimit` が 429 を返す）

**PUT /api/admin/articles/:slug**
- 200 + 更新結果
- 404 対象 slug なし

**DELETE /api/admin/articles/:slug**
- 200 削除成功
- 404 対象なし

**PATCH /api/admin/articles/:slug/toggle**
- 200 + 反転結果
- 404 対象なし

## 残タスク

| 区分 | 内容 | 見積 | 依存 |
|---|---|---|---|
| QA-T3 | `/api/ai` free/pro プラン別 3 ケース | 60分 | OpenRouter モック・db.users モック |
| QA-T4 | Playwright E2E 10シナリオ | 4〜6時間 | `playwright install chromium`・Google OAuth モック |
| Rev24 #② | `POST /api/auth/mobile/token` | 90分 | `jose` dep・Apple Services ID・Google モバイル Client ID |
| Rev24 #③ | Redis INCR + Vercel Cron | 60分 | Vercel Cron（Hobby プラン1本まで）|

## 検証コマンド

```bash
pnpm exec tsc --noEmit          # EXIT=0
pnpm lint                        # ✔ 0件
pnpm test                        # 9 files / 79 tests PASS
pnpm exec vitest --run --reporter=verbose  # 件名一覧（本ファイル作成に使用）
```
