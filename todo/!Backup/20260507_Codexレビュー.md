# React/Next.js 品質・ビルド安定性・初期表示性能

今回のレビュー観点: **React/Next.js 品質・ビルド安定性・初期表示性能**

::code-comment{title="[P1] 本番ビルドが依存関係不整合で失敗する" body="`npm run build` が `canvas-confetti` と `@vercel/speed-insights/next` を解決できず失敗しました。`package.json` と `pnpm-lock.yaml` には依存が入っていますが、`package-lock.json` には反映されていないため、npm 系の CI/Vercel ビルドでは同じ失敗が再現します。npm を使うなら `npm install` で `package-lock.json` を更新し、pnpm を使うなら `package-lock.json` を削除して CI/README/デプロイ設定を pnpm に統一してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\package.json" start=30 end=32 priority=1 confidence=0.97}

::code-comment{title="[P2] サイト全体を fallback=null の Suspense で包んでいる" body="`children` 全体を `<Suspense fallback={null}>` で包んでいるため、`useSearchParams()` 対策の CSR bailout がページ全体に広がりやすく、該当ルートで初期表示が空白になるリスクがあります。Suspense 境界は `LearnSearchBar` など `useSearchParams()` を使う小さな Client Component の直上に限定し、`children` 全体は包まない構造にしてください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\(site)\layout.tsx" start=21 end=27 priority=2 confidence=0.86}

::code-comment{title="[P2] AI教室ページ全体が巨大な Client Component になっている" body="`page.tsx` が `'use client'` で始まり、約1760行の UI、テーマ一覧、プレビュー、結果表示、チャット、保存処理まで同一クライアントバンドルに入ります。初期表示で不要な機能までダウンロード・ハイドレーションされるため、モバイルや低速回線で重くなります。ページ本体は Server Component に戻し、入力フォーム、チャット、PreviewPanel、ResultPanel などを小さい Client Component に分割し、重いパネルは `next/dynamic` で必要時ロードにしてください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\(site)\tools\ai-kyoshitsu\page.tsx" start=6 end=18 priority=2 confidence=0.9}

::code-comment{title="[P2] 記事本文レンダラーが Markdown/Highlight 処理を全てブラウザへ送っている" body="`ArticleBody` が Client Component で、`react-markdown`、`rehype-raw`、`rehype-sanitize`、`rehype-highlight` と複数言語定義をブラウザ側に含めています。記事本文は基本的にサーバーで HTML 化できるため、記事ページの JS が重くなります。Markdown のパース・sanitize・highlight は Server Component 側で行い、コピー按钮、注釈単語、iframe fullscreen など本当に対話が必要な部分だけ Client island に分離してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\components\article\ArticleBody.tsx" start=1 end=18 priority=2 confidence=0.88}

::code-comment{title="[P3] SSE の delta ごとに React state を更新している" body="AIチャットのストリーミング処理で `json.delta` を受け取るたびに `setMessages` を実行しており、長い回答では非常に多くの再レンダリングが発生します。`accumulated` は ref に保持し、`requestAnimationFrame` または 50〜100ms の throttle でまとめて state 更新する形にすると、入力・スクロール・描画の詰まりを抑えられます。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\components\article\AIChatWidget.tsx" start=492 end=500 priority=3 confidence=0.84}

確認結果:
- `lib/db/index.ts` は lazy 初期化されており、Next.js のビルド時 env 未設定クラッシュ対策はできています。
- 記事一覧や管理画面の一部クエリは `Promise.all` や debounce が使われており、基本方針は良いです。
- ただし現状は `npm run build` が失敗するため、この観点の品質点は **68/100** です。ビルド不整合を直すだけで大きく改善します。

# **UI/UX・アクセシビリティ**

今回のレビュー観点: **UI/UX・アクセシビリティ**

::code-comment{title="[P2] カスタムモーダルに dialog 属性とフォーカス管理がない" body="プレビュー、タイトル編集、削除確認の各モーダルが通常の `div` として実装されており、`role=\"dialog\"`、`aria-modal`、`aria-labelledby`、初期フォーカス、フォーカストラップ、閉じた後のフォーカス復帰がありません。キーボード利用者やスクリーンリーダー利用者は、モーダルが開いたことを認識しにくく、背後の操作要素へフォーカスが抜ける可能性があります。共通 `Dialog` コンポーネントを作り、開いたら見出しまたは最初の操作ボタンへフォーカス、Tab をモーダル内に閉じ込め、閉じたら呼び出し元ボタンへ戻す実装にしてください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\(site)\mypage\ai-kyoshitsu\AnimationList.tsx" start=128 end=134 priority=2 confidence=0.9}

::code-comment{title="[P2] 管理画面の検索・フィルターにラベルが不足している" body="会員管理の検索 input、プランフィルターボタン、ソート select が視覚的なプレースホルダーやボタン文言だけで構成されており、フォーム全体の意味がスクリーンリーダーに十分伝わりません。検索 input には `label` または `aria-label=\"会員を検索\"`、プラン切替には `role=\"radiogroup\"` と各ボタンの `aria-pressed` または radio 実装、select には `label` を付けてください。管理画面は運用者が長時間使うため、キーボード操作と読み上げ品質を優先すべきです。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\components\admin\AdminUserTable.tsx" start=160 end=211 priority=2 confidence=0.84}

::code-comment{title="[P3] alert/confirm が複数画面に残っている" body="AI Echo、AI教室履歴、管理画面などで `alert()` / `confirm()` が残っています。ブラウザ標準ダイアログはデザイン統一ができず、モバイルで文言が読みにくく、スクリーンリーダー向けの文脈やフォーカス復帰も制御できません。既に `AdminArticleTable` では aria-live トーストへ置換しているため、同じ方針でエラーは共通 Toast/InlineAlert、破壊的操作は共通 ConfirmDialog に統一してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\(site)\mypage\ai-kyoshitsu\AnimationList.tsx" start=518 end=610 priority=3 confidence=0.88}

::code-comment{title="[P3] AI Echo の textarea がプレースホルダー依存になっている" body="英文入力 textarea に明示的な `label`、`aria-label`、`aria-describedby` がなく、入力目的・制限・補助説明がフォームコントロールと紐づいていません。プレースホルダーは入力開始後に消えるため、特に長文入力や支援技術利用時に文脈を失いやすいです。各 Level の見出しや説明文に id を付け、textarea に `aria-labelledby` と `aria-describedby` を設定してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\components\voaenglish\AIEchoPanel.tsx" start=295 end=321 priority=3 confidence=0.82}

::code-comment{title="[P3] AIチャットの開閉でフォーカス復帰がない" body="チャットを開いたときは textarea にフォーカスしますが、閉じた後に開くボタンへフォーカスを戻していません。キーボード操作では閉じた直後に現在位置を見失いやすくなります。開くボタンの ref を保持し、閉じる処理で `openerRef.current?.focus()` を実行してください。可能ならチャット領域にも `aria-labelledby` を付け、開閉状態を `aria-expanded` で表現するとより安定します。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\components\article\AIChatWidget.tsx" start=581 end=616 priority=3 confidence=0.78}

この観点の点数は **74/100** です。大きな表示崩れというより、アクセシビリティと運用画面の操作品質に改善余地があります。次は **DB・スケール・データ取得性能** の観点で続けます。

# **DB・スケール・データ取得性能**

今回のレビュー観点: **DB・スケール・データ取得性能**

::code-comment{title="[P2] 公開記事検索が `%keyword%` ILIKE でインデックスを使いにくい" body="記事一覧 API と記事一覧ページの検索は `title` / `description` に対して `%...%` の `ILIKE` を使っています。この形は通常の btree index が効きにくく、記事数が増えると公開検索のたびに広範囲スキャンになります。Postgres なら `pg_trgm` を有効化し、`lower(title)` / `lower(description)` または対象列へ GIN trigram index を追加するか、検索要件が広がるなら `tsvector` の全文検索へ寄せてください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\api\articles\route.ts" start=145 end=152 priority=2 confidence=0.88}

::code-comment{title="[P2] 関連記事取得で `ORDER BY random()` を使っている" body="関連記事取得が `ORDER BY random()` になっており、候補行を並べ替えるためアクセス増加・記事数増加時に重くなります。記事詳細ページで毎回使われる導線なので、カテゴリ一致後に `publishedAt` / `viewCount` / `id` など indexed な条件で候補を絞るか、記事保存時に関連記事候補を事前計算する方式に変更してください。ランダム性が必要なら、全件 random sort ではなく候補IDを限定してからアプリ側でシャッフルする方が安全です。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\lib\repositories\articles.ts" start=101 end=112 priority=2 confidence=0.9}

::code-comment{title="[P2] ユーザー同期APIが全件取得・全件返却になっている" body="AIメモと単語ブックマークの GET はユーザーの全データを返し、ページネーションや差分同期がありません。長期利用で保存件数が増えると、ログイン直後やマイページ表示でレスポンスサイズとDB読み取りが増えます。`limit` / `cursor` / `updatedSince` を導入し、初回同期はページング、通常同期は差分取得にしてください。あわせて `user_id, saved_at` と `user_id, added_at` の複合インデックスを追加してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\api\user\ai-memos\route.ts" start=38 end=47 priority=2 confidence=0.86}

::code-comment{title="[P2] ブックマーク同期の bulk 上限が大きく、1リクエストのDB負荷が跳ねやすい" body="単語ブックマーク保存 API は最大2000件を1回の insert に流します。モバイル同期や再試行が増えると、単一リクエストが大きくなり、タイムアウト・メモリ増加・DBロック時間増加につながります。1リクエストの上限を 100〜300 件程度に下げ、クライアント側でチャンク送信する設計にしてください。サーバー側でも件数・総バイト数・各 text フィールド長を制限すると安定します。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\api\user\vocab-bookmarks\route.ts" start=75 end=103 priority=2 confidence=0.82}

::code-comment{title="[P3] 管理者ユーザー一覧の検索・ソート用インデックスが不足している" body="管理者ユーザー一覧は `email/name ILIKE`、`plan` 絞り込み、`createdAt/name/plan` ソート、複数の count を行いますが、`users` テーブルには email unique 以外の実用インデックスがありません。管理画面専用でも、ユーザー数が増えると検索や初期表示が遅くなります。`created_at DESC`、`plan`、必要なら `pg_trgm` による `email/name` 検索 index を追加し、サマリー集計は頻度が高ければキャッシュまたは集計テーブル化してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\api\admin\users\route.ts" start=48 end=88 priority=3 confidence=0.8}

確認できた良い点:
- `articles.categories` には GIN index があり、カテゴリ配列検索への配慮があります。
- `user_animations` は `user_id, created_at DESC` の複合 index migration があり、履歴一覧の基本性能は良いです。
- 進捗更新は `onConflictDoUpdate` で atomic に処理されており、同時更新に強いです。

この観点の点数は **76/100** です。小規模では問題になりにくいですが、公開検索・関連記事・同期APIはアクセス増加時に先に効いてくる可能性があります。次は **運用・監視・障害対応性** の観点で続けます。

#  **運用・監視・障害対応性観点**

今回のレビュー観点: **運用・監視・障害対応性**

::code-comment{title="[P2] CIで本番ビルドが実行されていない" body="CI は typecheck / lint / Vitest までで、`pnpm build` と smoke test がコメントアウトされています。そのため依存関係不整合、Next.js の静的生成エラー、Route Handler のバンドル漏れがPR時に検出されません。今回もローカルの `npm run build` で依存解決エラーが出ているため、最低限 `pnpm build` は必須ジョブに戻してください。DB等の実環境 secret が必要な smoke test は別ジョブでもよいですが、ビルドだけは CI で常時実行すべきです。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\.github\workflows\test.yml" start=30 end=43 priority=2 confidence=0.93}

::code-comment{title="[P2] API/AI処理のログが構造化されておらず相関IDがない" body="AI生成や API エラーのログはありますが、`x-vercel-id`、route、userId、plan、requestId、duration_ms、stage などが統一フォーマットで出ていません。障害時に「どのリクエストが、どのモデルで、どの段階で、何秒後に失敗したか」を追跡しにくいです。API Route 共通の `logInfo/logError` を作り、JSON structured log と `req.headers.get('x-vercel-id')` を必ず含めてください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\api\generate-animation\route.ts" start=616 end=623 priority=2 confidence=0.86}

::code-comment{title="[P2] 必須環境変数の起動時検証がない" body="`DATABASE_URL`、`NEXTAUTH_SECRET`、Google OAuth、OpenRouter、Upstash など必須/準必須の環境変数が各利用箇所で個別に参照され、未設定時は初回リクエストで初めて失敗します。Preview/Production の設定漏れを早期検出できません。`lib/env.ts` などで zod schema による環境変数検証を集約し、ビルド/起動/healthcheck で不足を明示してください。特に `NEXTAUTH_SECRET` と OAuth client secret は認証全体に影響するため fail-fast が必要です。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\lib\auth.ts" start=61 end=69 priority=2 confidence=0.84}

::code-comment{title="[P3] クライアントエラーの外部収集がなく console.error 止まり" body="Error Boundary では `console.error` のみで、本番のクライアント例外を Sentry 等へ送信していません。Vercel Runtime Logs だけではブラウザ側の例外・ユーザー環境・発生URL・頻度を追いにくいです。Sentry/Datadog などを導入し、`error.digest`、route、user/session の匿名ID、release version を付与して送信してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\app\(site)\error.tsx" start=16 end=20 priority=3 confidence=0.88}

::code-comment{title="[P3] AI設定DBの読み込み失敗が警告だけで運用検知されない" body="AI教室設定の DB レイヤー読み込みに失敗した場合、警告ログを出して DEFAULTS にフォールバックしています。ユーザー影響を抑える設計は良いですが、管理画面で変更した設定が反映されない状態を運用者が気づけない可能性があります。構造化ログに加えて、連続失敗時の alert、管理画面上の degraded 表示、`/api/health` で DB config layer の状態を返す仕組みを追加してください。" file="C:\Users\jun.li\OneDrive-UiPath\CC\Familyai.jp\lib\config\ai-config.ts" start=62 end=74 priority=3 confidence=0.8}

確認できた良い点:
- Vercel Speed Insights と GA は導入されています。
- 外部AIエラーはユーザー向けには 502/503/504 へ変換されており、最低限のUX保護はあります。
- DBクライアントは lazy 初期化されており、Preview build の env 未設定クラッシュ対策はされています。

この観点の点数は **70/100** です。実装機能は動いても、障害発生時の「検知・原因特定・再発防止」がまだ弱い状態です。