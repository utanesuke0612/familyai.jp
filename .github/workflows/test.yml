name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  static-and-unit:
    name: Static analysis + Vitest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript type check
        run: pnpm exec tsc --noEmit

      - name: ESLint
        run: pnpm lint

      - name: Vitest (unit tests)
        run: pnpm test

  # ──────────────────────────────────────────────────────────
  # Smoke test (Phase T1) — builds app and runs curl-based smoke
  # Commented out by default because production build requires
  # runtime env vars (DATABASE_URL, NEXTAUTH_SECRET, etc.).
  # Enable by adding encrypted secrets and uncommenting below.
  # ──────────────────────────────────────────────────────────
  # smoke:
  #   name: Smoke test (curl)
  #   runs-on: ubuntu-latest
  #   needs: static-and-unit
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with: { version: 10 }
  #     - uses: actions/setup-node@v4
  #       with: { node-version: 20, cache: pnpm }
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm build
  #       env:
  #         DATABASE_URL:     ${{ secrets.DATABASE_URL }}
  #         NEXTAUTH_SECRET:  ${{ secrets.NEXTAUTH_SECRET }}
  #         NEXTAUTH_URL:     http://localhost:3000
  #     - run: pnpm start &
  #       env:
  #         DATABASE_URL:    ${{ secrets.DATABASE_URL }}
  #         NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
  #     - run: npx wait-on http://localhost:3000
  #     - run: BASE_URL=http://localhost:3000 pnpm test:smoke
